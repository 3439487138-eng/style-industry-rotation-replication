# Pinned runtime/reporting dependencies for the repository wrapper.
# Private provider packages are intentionally excluded from the default install.
numpy==2.2.4
pandas==2.2.3
PyYAML==6.0.2
matplotlib==3.10.1
python-dotenv==1.1.0
pytest==8.3.5
akshare==1.18.96
