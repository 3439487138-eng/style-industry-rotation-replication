# Replication outputs

A successful current execution writes:

- `report.json`: auditable run evidence and report payload;
- `report.html`: standalone HTML with embedded figures.

Neither file is pre-populated because the available source and data cannot execute the full strategy. Test fixtures are written only to temporary test directories and must never be copied here.
