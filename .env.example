# Copy this file to .env for local use. Never commit .env.
# CLI options take precedence over these values, which take precedence over YAML.
REPLICATION_DATA_PATH=
REPLICATION_STRATEGY_ADAPTER=

# Optional legacy research modules (not used by run_replication.py).
PANDA_FACTOR_DATA_ROOT=
PANDA_FACTOR_LIBRARY_ROOT=
PANDA_FACTOR_MONGO_URI=
PANDA_FACTOR_MONGO_DB=

# Add provider credentials only after the real adapter declares the exact names
# in config/base.yaml under data.required_env. Never put credential values in YAML.
